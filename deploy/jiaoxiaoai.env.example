# Copy this file to deploy/jiaoxiaoai.env. Never commit the real file: it
# contains provider and tool-server credentials.

# Branding and test authentication. The first account remains the bootstrap
# administrator; later sign-ups become ordinary users immediately.
WEBUI_NAME=交小AI
JIAOXIAOAI_MANAGED_MODE=true
DEFAULT_USER_ROLE=user
ENABLE_SIGNUP=true

# Optional first-boot administrator. If omitted, the first locally registered
# account becomes Admin. Keep the password only in deploy/jiaoxiaoai.env.
# WEBUI_ADMIN_EMAIL=admin@example.edu.cn
# WEBUI_ADMIN_NAME=交小AI管理员
# WEBUI_ADMIN_PASSWORD=replace-with-a-strong-password

# Optional jAccount/OIDC integration. Uncomment only after registering the
# callback URL (normally https://your-host/oauth/oidc/callback) with jAccount.
# OAUTH_PROVIDER_NAME=jAccount
# ENABLE_OAUTH_SIGNUP=true
# OAUTH_CLIENT_ID=replace-with-jaccount-client-id
# OAUTH_CLIENT_SECRET=replace-with-jaccount-client-secret
# OPENID_PROVIDER_URL=https://jaccount-provider.example/.well-known/openid-configuration
# OPENID_REDIRECT_URI=https://your-host/oauth/oidc/callback
# OAUTH_SCOPES=openid email profile

# Administrator-owned provider. Students never receive this key.
ENABLE_OLLAMA_API=false
ENABLE_OPENAI_API=true
# The default embedding model is baked into the image. Avoid a remote update
# check during startup; Knowledge settings remain editable in the Admin UI.
RAG_EMBEDDING_MODEL_AUTO_UPDATE=false
OPENAI_API_BASE_URL=https://your-provider.example/v1
OPENAI_API_KEY=replace-me

# One-time managed model bootstrap. JIAOXIAOAI_BASE_MODEL_ID must be an ID
# returned by the provider above. Existing model rows are never overwritten.
JIAOXIAOAI_BASE_MODEL_ID=replace-with-provider-model-id
JIAOXIAOAI_MODEL_ID=jiaoxiaoai
JIAOXIAOAI_MODEL_NAME=交小AI
JIAOXIAOAI_MODEL_PARAMS={}
# After Knowledge/Tools exist, their stable IDs can be recorded here for a
# fresh database. Example tool IDs: local-tool-id or server:mcp:mcp-server-id.
JIAOXIAOAI_MODEL_METADATA={"capabilities":{"builtin_tools":true,"citations":true,"file_context":true,"usage":true,"web_search":true},"defaultFeatureIds":[],"knowledge":[],"toolIds":[]}
DEFAULT_MODELS=jiaoxiaoai
DEFAULT_PINNED_MODELS=jiaoxiaoai
MODEL_ORDER_LIST=["jiaoxiaoai"]

# Declarative Knowledge and MCP. Mount deploy/managed read-only; secrets are
# referenced by ${ENV_NAME} in YAML and supplied only in this private env file.
JIAOXIAOAI_MANAGED_KNOWLEDGE_DIR=/app/managed/knowledge
JIAOXIAOAI_MANAGED_MCP_DIR=/app/managed/mcp
AMAP_MCP_KEY=replace-with-amap-mcp-key
BOCHA_API_KEY=replace-with-bocha-api-key

# Built-in Web Search stays off because managed MCP provides web search.
# Admin may explicitly enable and configure the built-in provider as a fallback.
ENABLE_WEB_SEARCH=false
USER_PERMISSIONS_FEATURES_WEB_SEARCH=true

# Platform configuration remains administrator-owned.
ENABLE_DIRECT_CONNECTIONS=false
ENABLE_API_KEYS=false
USER_PERMISSIONS_WORKSPACE_MODELS_ACCESS=false
USER_PERMISSIONS_WORKSPACE_KNOWLEDGE_ACCESS=false
USER_PERMISSIONS_WORKSPACE_PROMPTS_ACCESS=false
USER_PERMISSIONS_WORKSPACE_TOOLS_ACCESS=false
USER_PERMISSIONS_WORKSPACE_SKILLS_ACCESS=false
USER_PERMISSIONS_FEATURES_DIRECT_TOOL_SERVERS=false
USER_PERMISSIONS_FEATURES_API_KEYS=false

# Keep the student navigation focused on chat for phase one. These can be
# enabled later without changing the authentication integration.
ENABLE_FOLDERS=false
ENABLE_NOTES=false
ENABLE_CHANNELS=false
ENABLE_MEMORIES=false
ENABLE_AUTOMATIONS=false
ENABLE_CALENDAR=false
USER_PERMISSIONS_FEATURES_FOLDERS=false
USER_PERMISSIONS_FEATURES_NOTES=false
USER_PERMISSIONS_FEATURES_CHANNELS=false
USER_PERMISSIONS_FEATURES_MEMORIES=false
USER_PERMISSIONS_FEATURES_AUTOMATIONS=false
USER_PERMISSIONS_FEATURES_CALENDAR=false

# Production deployments must set a stable random value.
WEBUI_SECRET_KEY=replace-with-a-long-random-secret
