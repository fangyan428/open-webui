services:
  open-webui:
    env_file:
      - ./deploy/jiaoxiaoai.env
    environment: !reset []
    ports: !override
      - "3001:8080"
    volumes: !override
      - ./deploy/jiaoxiaoai-data:/app/backend/data
